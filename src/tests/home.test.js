import { test, expect } from '@jest/globals';

test('should check if data is displayed on the home page', () => {

    const dataIsDisplayed = true;
  
    expect(dataIsDisplayed).toBe(true);
  });
  